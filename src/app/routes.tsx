// Step 1 placeholder - routes will be implemented from Step 2 onward.
// Keep as minimal stub to satisfy folder structure (spec §25).

export const routes = [] as const;

export type AppRoute = (typeof routes)[number];
